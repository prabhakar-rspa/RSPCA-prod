<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LeadToAccountRecordType__c</fullName>
    <description>Used to map the account record type from the lead to the account record. Used for validation to ensure that lead gets converted to the correct account type.</description>
    <externalId>false</externalId>
    <inlineHelpText>Used to map the account record type from the lead to the account record. Used for validation to ensure that lead gets converted to the correct account type.</inlineHelpText>
    <label>LeadToAccountRecordType</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetName>Account_Type</valueSetName>
    </valueSet>
</CustomField>
